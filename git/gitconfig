[core]
    excludesfile=~/.gitignore
    editor=vim

[diff]
    renameLimit=10000

[user]
    name = "Pierre-Yves Caneill"
    email = "xt3ems@gmail.com"

[alias]
    co=checkout
    ci=commit
    st=status
    stat=status
    pop=reset HEAD~
    undo=reset HEAD~
    glog=log --graph

    amend = commit --amend
    squash = commit --amend -C HEAD
    fixup = commit --amend -C HEAD

    unstash = stash apply

[color]
    ui     = auto
    diff   = auto
    branch = auto
    status = auto
    pager  = true
    interactive = auto

[color "diff"]
    meta = green
    frag = yellow
    old = magenta
    new = bold cyan

[color "status"]
    header = bold blue
    added  = green
    changed = bold red
    untracked = bold yellow

[rebase]
    stat = true

[push]
    default = current
